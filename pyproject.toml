[project]
name = "vicsek-viz"
version = "0.1.0"
description = "Visualization and analysis tools for the Vicsek flocking model"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "matplotlib",
    "ovito>=3.10",
]
